<div class="container">
  <div class="row justify-content-center">
    <div class="col-10  text-light">
      <%= render 'shared/error_messages', obj: @category %>
      <%= form_with(model: @category, class: "shadow p-3 mb-3 bg-info rounded", local: true) do |f|%>

      <div class="form-group row">
        <%= f.label :name, "Category Name:", class: "col-2 col-form-label" %>
        <div class="col-10">
        <%= f.text_field :name, class: "form-control shadow rounded", placeholder: "Enter a Category name" %>
        </div>
      </div>

      <div class="container-fluid">
        <div class= "form-group row justify-content-center">
          <div class="col-6">
          <%= f.submit class: "btn btn-outline-light btn-block"%>
          </div>
        </div>

        <div class="row justify-content-center">
          <div class="col-6">
            <%= link_to "Cancel", articles_path, class: "btn btn-outline-danger btn-block" %>
          </div>
        </div>
      </div>
      <% end %>

    </div>
  </div>
</div>